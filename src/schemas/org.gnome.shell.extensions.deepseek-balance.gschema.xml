<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
    <schema id="org.gnome.shell.extensions.deepseek-balance" path="/org/gnome/shell/extensions/deepseek-balance/">
        <key name="refresh-interval" type="i">
            <default>300</default>
            <range min="60" max="3600"/>
            <summary>Refresh interval in seconds</summary>
            <description>How often, in seconds, the extension refreshes balance data from the DeepSeek API. 300 seconds (5 minutes) is recommended to avoid API rate limiting.</description>
        </key>
        <key name="currency" type="s">
            <default>''</default>
            <summary>Preferred display currency</summary>
            <description>Currency code to display; empty selects the first balance returned by the API.</description>
        </key>
    </schema>
</schemalist>
